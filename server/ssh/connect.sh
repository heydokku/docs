-  If the root Account Uses Password Authentication
ssh dokku@your_server_ip

-  If the root Account Uses 
rsync --archive --chown=deploy:deploy ~/.ssh /home/dokku

```sh
# local server
ssh dokku@your_server_ip 
sudo command
```
